---
const { nomMaison, adresse, favori, nbChambres, nbSdb, prix, surface, imgUrl } =
  Astro.props;
---

<!--
<div x-data=`{ favori: ${favori}}` x-bind:class="{'bg-yellow-200': favori}">
  <ul>
    <li>{nomMaison}</li>
    <li>{adresse}</li>
    <li>{favori}</li>
    <li>{nbChambres}</li>
    <li>{nbSdb}</li>
    <li>{prix}</li>
    <li>{surface}</li>
    <li><img src={imgUrl} alt="" /></li>
    <button x-on:click="favori = !favori">Favori</button>
  </ul>
</div>
-->
<div class="flex align-center justify-center">
  <div class="flex flex-col w-1/2">
    <div>
      <img src={imgUrl} alt="" />
    </div>
    <div>
      <div class="flex justify-between">
        <p>{prix}/month</p>
        <p>{nomMaison}</p>
      </div>
      <div>{favori}</div>
    </div>
  </div>
</div>
